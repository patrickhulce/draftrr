{
  "name": "@draftrr/core",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build:data": "tsx scripts/build-data.ts",
    "build:ts": "tsc -p tsconfig.json",
    "build": "pnpm run build:data && pnpm run build:ts",
    "lint": "eslint src scripts && prettier --check src scripts",
    "lint:fix": "eslint --fix src scripts && prettier --write src scripts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@draftrr/wire": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
